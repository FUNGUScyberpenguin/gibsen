{
  "name": "gibsen-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser-based studio for turning cyber threat intelligence into GIBSEN diagrams",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  }
}
